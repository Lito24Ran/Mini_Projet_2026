#:import PageAcc pageAcc
#:import Mymap map2
#:import NavigationScreenManager Navigation_screen_Manager
#:import ProgressBarWidget Progress_bar

#ProgressBarWidget:
#Mymap:
PageAcc:

#MyScreenManager:

<MyScreenManager@NavigationScreenManager>:
    Screen:
        name: "Screen1"
        Button:
            text:"Aler vers ma map"
            on_press: root.push("Mymap") #Eto zany dia mandeha ao @ le fonction push ilay Button d pusheny ao anaty tableau ilay izy
    Screen:
        name: "Mymap"
        Mymap:

    Screen:
        name: "Screen2"
        Button:
            text:"Button2"
            on_press: root.pop()
    